package com.model;

public class SquarePerimetry {

    public double calculation(double length) {
        double perimetry = length * 4;
        System.out.println("Perimetry = " + perimetry);
        return perimetry;
    }
}
